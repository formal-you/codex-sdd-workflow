# 流程摘要 (Process Summary)

本文件用于维护**长期项目视角**与**归档摘要**。当前执行状态和共享 Session Handoff 请维护在 `progress.md`；branch-local 或 task-local 的细节便签请维护在 `../state/hot/`。

用 `[ ]` 标记未完成、待推进或待确认事项，用 `[x]` 标记已完成或已确认事项。

## Macro Focus

- [ ] 当前阶段目标 (Milestone)：
- [ ] 核心关注点：

## Rules of Archiving

- [x] 当前工作请维护在 `progress.md` 中。
- [x] branch-local 或 task-local 的细节便签请维护在 `../state/hot/` 中，不要让 `progress.md` 重新膨胀成大杂烩。
- [x] Task 完结后，将其移动到 `tasks/history/`，然后在此处添加一行摘要。
- [x] 归档任务前，必须在 `progress.md` 的 Session Handoff 区域指向后续动作。
- [x] task 级热状态不再需要时，请移动到 `../state/history/`，或压缩回共享摘要。

## Recent History

> 这里只写最重要的近期流水账，保持精简。

- [ ] `tasks/history/TASK-001.md` —— 完成了核心 API 改造

## Pointers

- [ ] active tasks：`tasks/active/`
- [ ] 历史归档：`tasks/history/`
- [ ] 热状态：`state/hot/`
- [ ] 热状态历史：`state/history/`
- [ ] 架构决策：`adr/records/`
- [ ] 验证证据：`evidence/records/`
