# Releases

- `records/` stores release plans and release checklists
- use `../scripts/new-release.ps1` or `.sh` to create a release card
- release cards should link the sprint, tasks, evidence, and deployment checks
