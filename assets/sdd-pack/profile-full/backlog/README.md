# Backlog

- `items/` holds ordered backlog cards
- use `../scripts/new-backlog-item.ps1` or `.sh` to create new entries
- link promoted backlog items to sprint cards and task cards
- when `EXTERNAL_ISSUE_SOURCE` is not `none`, treat backlog cards here as execution mirrors and handoff carriers rather than the only source of truth
