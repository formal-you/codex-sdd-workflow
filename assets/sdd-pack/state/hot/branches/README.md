# Branch Hot State

Keep branch-local scratch notes here.

## Naming

- prefer one markdown file per branch
- use a safe filename derived from the branch name
- replace `/` with `__` and other unsafe characters with `-`

Example:

- branch `feature/login-flow` -> `feature__login-flow.md`
