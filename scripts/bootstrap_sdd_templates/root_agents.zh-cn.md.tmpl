# AGENTS.md

本仓库把可恢复的 workflow 状态放在 [`{sdd_dir}/`](./{sdd_dir}/) 下。把本文件视为唯一稳定的 agent 工作契约。

## Ground Truth 优先级

当信息冲突时，按以下顺序信任：

1. 运行中的代码、编译器输出与测试结果
2. 当前 git diff，以及 active task/subtask 文件
3. [`{sdd_dir}/docs/`](./{sdd_dir}/docs/) 下的项目文档
4. 旧笔记、聊天历史与推测

## 会话启动清单

1. 阅读 [`{sdd_dir}/docs/process.md`](./{sdd_dir}/docs/process.md)
2. 阅读 [`{sdd_dir}/docs/progress.md`](./{sdd_dir}/docs/progress.md)
3. 阅读当前 task 以及关联 subtasks
4. 执行 `git rev-parse --show-toplevel`
5. 执行 `git status --short`
6. 执行 `./{sdd_dir}/scripts/session-brief.ps1` 或 `./{sdd_dir}/scripts/session-brief.sh`
7. 执行 `./{sdd_dir}/scripts/validate-sdd.ps1 -Strict` 或 `./{sdd_dir}/scripts/validate-sdd.sh --strict`
8. 阅读 [`{sdd_dir}/workflow.md`](./{sdd_dir}/workflow.md) 以及与你当前改动相关的文档

## Task、Subtask 与集成规则

- 从 task card 开始实现，不要只依赖聊天记忆。
- 如果用户提出多个任务，或者一个任务可以拆成多个独立可验收的工作单元，主 agent 必须先评估并行拆分，并先在 parent task 中写下拆分判断。
- 当 owned scope 可分离、验收标准可独立验证、且最终集成仍由主 agent 负责时，默认优先创建 subtasks，并在当前会话允许时使用多个 subagents。
- 如果写入范围重叠过多、顺序依赖严格、或关键路径被某个 subtask 阻塞，就不要并行。
- 每个被委派的 subtask 都要从 parent task card 链接出去。
- 如果仓库 workflow 期望委派，但当前会话策略不允许 `spawn_agent`，也要明确记录“已评估并行，但受当前会话策略限制未委派”。
- 主 agent 负责集成、最终验证、风险总结与 workflow 更新。

## Workflow Artifact 规则

- 当 [`{sdd_dir}/scripts/`](./{sdd_dir}/scripts/) 下存在本地 helper 时，优先使用脚本创建 workflow artifact 的初始 scaffolding，而不是手写结构。
- 用 `new-task` 在 `tasks/active/` 中创建 parent task card。
- 用 `new-subtask` 在 `tasks/active/` 中创建 delegated subtask。
- 用 `new-adr` 记录持久化技术决策。
- 已完成的 task 或 subtask 要从 `tasks/active/` 归档到 `tasks/history/`，并同步更新 `docs/process.md`。
- 输出 session handoff 时使用 `handoff-template`。
- 如果某类 artifact 有本地脚本，先用脚本创建，再手动补内容。

## Git Workflow

请遵循 [`{sdd_dir}/docs/git-workflow.md`](./{sdd_dir}/docs/git-workflow.md) 中的分支命名、Conventional Commits 和 PR 准备规则。
验证通过后，遵循 [`{sdd_dir}/workflow-config.env`](./{sdd_dir}/workflow-config.env) 中的 `TASK_COMPLETION_GIT_MODE`：只有在策略允许时才提交，否则记录未提交原因并给出推荐 commit message。

## 完成定义

1. 代码改动完成
2. 已运行目标测试或检查
3. 文档、任务状态与风险已更新
4. Git handoff 已记录：已提交，或未提交并说明原因和推荐 commit message
5. 分支与提交已达到可 handoff 或可发起 PR 的状态
